server:
  port: 8296
spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Velarian/config-server
          clone-on-start: true
